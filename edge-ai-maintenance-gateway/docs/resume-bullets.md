# Resume Bullets

- Built an embedded Linux edge gateway that ingests TinyML maintenance
  telemetry, buffers it in SQLite, and generates local alarm/dashboard reports.
- Implemented JSONL replay, HTTP API route contracts, alarm-event persistence,
  summary queries, and systemd deployment documentation.
- Designed the gateway so future UART/MQTT ingestion can reuse the same storage
  and dashboard pipeline.

