# Keep dependency truth in pyproject.toml.
#
# This file exists for tools and Docker flows that still expect a
# requirements.txt entrypoint. Installing the local package with the [api]
# extra pulls the FastAPI runtime without duplicating version pins here.
.[api]
