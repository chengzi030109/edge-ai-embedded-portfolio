# Resume Bullets

- Built an embedded Linux visual inspection demo with synthetic defect images,
  lightweight image features, anomaly scoring, annotated outputs, and reports.
- Designed the app to support future USB camera/RTSP input and ONNX detectors
  without changing the batch/report contract.
- Added API route contracts, systemd deployment notes, synthetic fixtures, and
  pytest coverage.

