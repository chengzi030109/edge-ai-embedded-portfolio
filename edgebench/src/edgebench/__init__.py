"""Edge AI benchmark utility.

Only package metadata lives here. Runtime behavior is implemented in dedicated
modules such as ``cli``, ``runner``, ``backends``, and ``report`` so future
agents can find responsibilities quickly.
"""

__version__ = "0.1.0"
